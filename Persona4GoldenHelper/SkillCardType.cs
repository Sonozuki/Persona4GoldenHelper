namespace Persona4GoldenHelper
{
    /// <summary>The types of skill cards.</summary>
    public enum SkillCardType
    {
        /// <summary>The skill is active physical based.</summary>
        Physical,

        /// <summary>The skill is active magical based.</summary>
        Magical,

        /// <summary>The skill is support based (e.g. healing).</summary>
        Support,

        /// <summary>The skill is passive.</summary>
        Passive
    }
}
